int n = 2;
int m = 9;
bool isMirror = true;

for (int i = n; i < m; i++; x == 0;remp)
{
    for (int j = i - 1; j > 1; j--)
    {
        do
        {
            (i != '0');
            x = i % 10;
            reverse = reverse * 10 + x;
            i = i; // 10;


        }
        while (i == reverse);
        isMirror = true;

        isMirror = false;
    }
}   
